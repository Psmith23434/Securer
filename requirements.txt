# Core pipeline — no external deps (uses stdlib ast)
# Test runner
pytest>=7.4.0
pytest-cov>=4.1.0

# For final build stage (install separately when building)
# nuitka>=2.0
# Cython>=3.0

# For snippet_tool OCR features (app-level, not obfuscator)
# pytesseract>=0.3.10
# Pillow>=10.0
# easyocr>=1.7
# customtkinter>=5.2
