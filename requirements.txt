# GUI
customtkinter>=5.2
tkinterdnd2>=0.3.0

# Development / testing
pytest>=8.0

# Final compilation (install separately — not a pip package on all systems)
# nuitka>=2.0
# Requires MSVC Build Tools on Windows:
#   https://visualstudio.microsoft.com/visual-cpp-build-tools/
